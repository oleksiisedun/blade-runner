Run the push script to deploy a subproject to Google Apps Script via clasp, commit+push changes to git, and optionally send email notifications.

Before running the script, ask the user: "Send deployment notification? (y/n)"
Then run: bash .claude/scripts/push.sh <answer>
where <answer> is "y" or "n" based on their response.

ARGUMENTS: $ARGUMENTS
